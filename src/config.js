// src/config.js
// Central place for site-wide configuration values.

// Base URL for media (videos, large assets) hosted on Cloudflare R2.
// Update this ONE place when switching from the R2 public dev URL
// to the final custom domain (e.g. after DNS migration is complete).
export const R2_BASE_URL = 'https://pub-cef711bfec094bd188b2bb0209748035.r2.dev';